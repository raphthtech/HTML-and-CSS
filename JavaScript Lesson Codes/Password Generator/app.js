const randomPassword = (){
    let char = "abcdefghijklmnopqrstuvwxyz0123456789!@#$%&*()_+~|{}[]:?<>,./-=\";
    let password = '';

    for(let i = 0; i < char.length; i++){
        let randomIndex = Math.floor(Math.random()*char.length);
    }
    
}