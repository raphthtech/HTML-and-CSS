// LOOP OVER STRINGS

// Loop over the string ABC

var letters = 'abc';

for(var i = 0; i < letters.length; i++) {
    console.log(letters[i]+'first letter')
}

ARRAYS ARE ITERABLE

var veggies = [
    'onions', 'carrot','pear'
]